{
  "name": "cassette-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cassette record/replay gateway — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
